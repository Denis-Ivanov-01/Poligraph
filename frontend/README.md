# Frontend

React/Vite public frontend for the Political AI Filter MVP.

## Commands

```bash
npm install
npm run dev
npm run build
```

The app reads `VITE_API_BASE_URL` and calls only public read-only `/api/*` endpoints.
